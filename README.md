## This repo consists code of screen recording feature where user can capture and record the screen along with systems + user's audio
- To run this app clone this repo
- `npm run dev` to start the app
 
